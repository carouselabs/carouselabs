// /admin/settings — app-level settings.
import { SettingsForm } from "@/components/admin/SettingsForm"

export const dynamic = "force-dynamic"

export default function AdminSettingsPage() {
  return <SettingsForm />
}
